{
  "name": "job-sheet",
  "version": "0.1.0",
  "private": true,
  "description": "Capture job postings into a Google Sheets application tracker.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "package": "bash scripts/package.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
