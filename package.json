{
  "name": "job-sheet",
  "version": "0.3.0",
  "private": true,
  "description": "Capture job postings into a Google Sheets application tracker.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "package": "bash scripts/package.sh",
    "package:bootstrap": "bash scripts/package.sh --bootstrap"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
